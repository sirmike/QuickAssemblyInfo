This tool installs as a Windows menu helper command and displays basic info about dependencies of selected C# assembly.

I created it when .NET Framework 2.x was a standard (something around 2009). It's quite possible that it will not work anymore with newer .NET Framework versions.
